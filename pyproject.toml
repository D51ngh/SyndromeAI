[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qec-fault-forensics"
version = "0.1.0"
description = "Diagnosing circuit-level surface-code faults from detector histories"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
